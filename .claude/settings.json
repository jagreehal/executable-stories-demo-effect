{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "if": "Edit(*.ts)|Write(*.ts)",
            "command": "f=$(jq -r .tool_input.file_path); ! grep -q \"from 'effect'\" \"$f\" || npx effect-analyze \"$f\" --quiet",
            "timeout": 30,
            "statusMessage": "Regenerating effect diagram"
          }
        ]
      }
    ]
  }
}
